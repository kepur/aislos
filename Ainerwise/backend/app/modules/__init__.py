"""Business-domain modules owned by Ainerwise Core.

Existing flat ``models`` and ``services`` packages remain available during the
incremental migration. New cross-endpoint domain rules belong here.
"""
