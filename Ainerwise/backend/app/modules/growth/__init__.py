"""Growth module: sourcing → localisation → auto-pricing → syndication.

Contracts (DTOs / Protocols / pricing formula) live in ``contracts``;
persistence in ``models``. See ``docs/GROWTH_SOURCING_SYNDICATION_STANDARD.md``.
"""
