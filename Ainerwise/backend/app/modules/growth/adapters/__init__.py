"""Growth provider adapters.

Each adapter implements one capability Protocol from ``..contracts`` and is
registered (as a spec) in ``..registry_boot``. Swapping a vendor = adding a
file here; callers never hard-code a vendor name.
"""
