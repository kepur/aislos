<template><div class="space-y-4 p-4"><div class="flex justify-between"><h1 class="text-xl font-bold text-slate-800">广告</h1><NuxtLink to="/supplier/ads/create" class="text-sm font-semibold text-blue-600">Create</NuxtLink></div><div v-for="item in items" :key="item.id" class="m-card"><p class="font-semibold">{{ item.title }}</p><p class="text-xs text-slate-400">{{ item.status }} · {{ item.clicks }} clicks</p></div></div></template>
<script setup lang="ts">definePageMeta({middleware:['auth']});const {apiFetch}=useApi();const items=ref<any[]>([]);onMounted(async()=>items.value=(await apiFetch<any>('/cebu-trade/ads/campaigns')).items)</script>
