<template><section class="space-y-5"><h1 class="text-2xl font-bold text-white">{{ $t('sup.reviewsTitle') }}</h1><div v-for="item in items" :key="item.id" class="pc-card"><p class="font-semibold text-amber-300">{{ '★'.repeat(item.rating) }}</p><p class="mt-2 text-slate-300">{{ item.comment || $t('sup.noComment') }}</p></div><p v-if="!items.length" class="pc-card text-slate-400">{{ $t('sup.noReviews') }}</p></section></template>
<script setup lang="ts">definePageMeta({layout:'procurement',middleware:['auth']});const {listSupplierReviews}=useCommerce();const items=ref<any[]>([]);onMounted(async()=>items.value=(await listSupplierReviews()).items)</script>
